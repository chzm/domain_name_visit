{
  "name": "domain_name_visit",
  "version": "1.0.0",
  "description": "My premium Nuxt.js project",
  "author": "rattanchen",
  "private": true,
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "8012"
    }
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0",
    "@nuxtjs/axios": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
